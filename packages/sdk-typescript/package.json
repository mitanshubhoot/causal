{
  "name": "@causal/sdk",
  "version": "0.1.0",
  "description": "Causal SDK — automatic provenance capture for TypeScript AI agent projects",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@causal/types": "workspace:*",
    "uuidv7": "^1.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.3",
    "vitest": "^4.1.10"
  },
  "peerDependencies": {
    "zod": "^3.0.0"
  }
}
